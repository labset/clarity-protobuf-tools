package {{ .Package }}

import (
	"github.com/gofrs/uuid/v5"
	"github.com/jackc/pgx/v5/pgtype"
	"google.golang.org/protobuf/types/known/timestamppb"

	pluginv1 "github.com/labset/clarity-protobuf-tools/api/clarity/plugin/v1"
	"{{ .StoreImport }}"
	{{ .ProtoAlias }} "{{ .ProtoImport }}"
)

func {{ .ModelLower }}ToProto(row db.{{ .DBPrefix }}{{ .Model }}) *{{ .ProtoAlias }}.{{ .Model }} {
	return &{{ .ProtoAlias }}.{{ .Model }}{
		Entity: &pluginv1.Entity{
			Id:        row.ID.String(),
			CreatedAt: timestamppb.New(row.CreatedAt.Time),
			UpdatedAt: timestamppb.New(row.UpdatedAt.Time),
		},
{{- range .Fields }}
		{{ .ProtoName }}: row.{{ .SQLCName }},
{{- end }}
	}
}

func {{ .ModelLower }}FromCreate(msg *{{ .ProtoAlias }}.{{ .Model }}, id uuid.UUID, now pgtype.Timestamptz) db.Create{{ .DBPrefix }}{{ .Model }}Params {
	return db.Create{{ .DBPrefix }}{{ .Model }}Params{
		ID:        id,
		CreatedAt: now,
		UpdatedAt: now,
{{- range .Fields }}
		{{ .SQLCName }}: msg.Get{{ .ProtoName }}(),
{{- end }}
	}
}

func {{ .ModelLower }}FromUpdate(msg *{{ .ProtoAlias }}.{{ .Model }}, id uuid.UUID) db.Update{{ .DBPrefix }}{{ .Model }}Params {
	return db.Update{{ .DBPrefix }}{{ .Model }}Params{
		ID: id,
{{- range .Fields }}
		{{ .SQLCName }}: msg.Get{{ .ProtoName }}(),
{{- end }}
	}
}
