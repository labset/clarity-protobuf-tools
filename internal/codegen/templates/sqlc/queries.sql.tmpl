-- name: Get{{ .MessageName }} :one
SELECT {{ .AllColumns }}
FROM {{ .Schema }}.{{ .Table }}
WHERE id = $1;

-- name: List{{ .MessageName }}s :many
SELECT {{ .AllColumns }}
FROM {{ .Schema }}.{{ .Table }};

-- name: Create{{ .MessageName }} :exec
INSERT INTO {{ .Schema }}.{{ .Table }} (
  {{ .AllColumns }}
) VALUES (
  {{ .AllPlaceholders }}
);

-- name: Update{{ .MessageName }} :exec
UPDATE {{ .Schema }}.{{ .Table }}
SET {{ .UpdateSetClause }}
WHERE id = $1;

-- name: Delete{{ .MessageName }} :exec
DELETE FROM {{ .Schema }}.{{ .Table }}
WHERE id = $1;
